import sys
a = sys.abiflags  # expects: str
b = sys.argv  # expects: [str]
c = sys.base_exec_prefix  # expects: str
d = sys.base_prefix  # expects: str
e = sys.byteorder  # expects: str
f = sys.builtin_module_names  # expects: [str]
g = sys.copyright  # expects: str
h = sys.hexversion  # expects: int
i = sys.path  # expects: [str]
j = sys.platform  # expects: str
k = sys.prefix  # expects: str
l = sys.ps1  # expects: str
m = sys.ps2  # expects: str
n = sys.version  # expects: str
o = sys.api_version  # expects: int